/// Defines breakpoints for device types used in context extensions.
class ContextBreakpoints {
  /// Maximum width for mobile devices.
  static const double mobile = 600;

  /// Minimum width for tablets, maximum for tablets.
  static const double tablet = 1200;
}
